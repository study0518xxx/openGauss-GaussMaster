# 01-项目概览与架构总览

> 本文档是 GaussMaster 项目的总览，帮助你快速建立全局认知。

---

## 1. 项目定位

**GaussMaster** 是华为开源的**数据库智能运维 Copilot 平台**，基于大语言模型（LLM）和人工智能技术，为数据库管理员（DBA）提供一站式的智能运维解决方案。

核心目标：**通过自然语言交互，实现数据库的智能问答、根因诊断、故障巡检、预测调优等全生命周期运维管理。**

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户交互层                                 │
│                    (Web UI / API)                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      GaussMaster 服务层                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │  智能问答    │  │  工具交互    │  │  多Agent     │           │
│  │  (Q&A)      │  │  (Tools)    │  │  (Agent)     │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │  向量检索    │  │  知识库管理  │  │  会话管理    │           │
│  │  (Vector)   │  │  (KB)        │  │  (Session)   │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
└─────────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   DBMind服务    │ │   向量数据库    │ │   大模型服务    │
│ (openGauss管控) │ │ (知识检索)      │ │ (LLM推理)       │
└─────────────────┘ └─────────────────┘ └─────────────────┘
          │                   │                   │
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   openGauss     │ │   Embedding     │ │   Reranker      │
│   数据库实例    │ │   模型服务      │ │   模型服务      │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### 2.2 架构分层

| 层级 | 组件 | 说明 |
|------|------|------|
| **表现层** | Web UI / REST API | 用户交互接口 |
| **服务层** | GaussMaster Core | 核心业务逻辑处理 |
| **模型层** | LLM + Embedding + Reranker | AI模型推理 |
| **数据层** | 向量数据库 + 元数据库 | 知识存储与检索 |
| **管控层** | DBMind | openGauss实例管理 |
| **数据库层** | openGauss | 目标数据库 |

---

## 3. 两大核心流程

### 3.1 RAG 智能问答流程 (ask_gauss)

```
用户提问
    │
    ▼
┌─────────────┐
│  参数校验    │  ← 检查question是否为空
└─────────────┘
    │
    ▼
┌─────────────┐
│ 敏感词检测   │  ← DFA算法检测不安全内容
└─────────────┘
    │
    ▼
┌─────────────┐     ┌─────────────┐
│  向量检索    │  +  │  文本检索    │  ← 双路召回
└─────────────┘     └─────────────┘
    │
    ▼
┌─────────────┐
│  Reranker   │  ← 重排序优化
└─────────────┘
    │
    ▼
┌─────────────┐
│  LLM生成答案 │  ← 流式输出
└─────────────┘
    │
    ▼
┌─────────────┐
│ 保存到数据库 │  ← SQLite持久化
└─────────────┘
```

**核心代码**: `server/web/data_transformer.py` 中的 `ask_gauss()` 和 `search()`

### 3.2 Agent 工具调用流程 (intelligent_interaction)

```
用户提问
    │
    ▼
┌─────────────────┐
│ 1. infer_tool_name │  ← 意图识别，选择工具
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 2. check_has_valid_tool │  ← 校验工具是否存在
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 3. check_is_no_param_tool │  ← 检查是否需要参数
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 4. infer_arguments │  ← 参数提取
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 5. verify_arguments │  ← 参数校验
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 6. call_tool │  ← 执行工具
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 7. 格式化输出 │  ← 返回结果
└─────────────────┘
```

**核心代码**: `llms/executor.py` 中的工具调用链路

---

## 4. 核心模块职责

| 模块 | 路径 | 职责 | 代码行数 |
|------|------|------|---------|
| **common** | `common/` | 公共组件（HTTP、数据库、安全等） | ~5,301 行 |
| **multiagents** | `multiagents/` | 多Agent系统（核心业务） | ~3,081 行 |
| **utils** | `utils/` | 工具函数（文档处理、检索等） | ~2,071 行 |
| **server** | `server/` | Web服务层 | ~1,184 行 |
| **llms** | `llms/` | 大模型集成 | ~772 行 |
| **controllers** | `controllers/` | 控制器层（API路由） | ~359 行 |

**总规模**: 100 个 Python 文件，13,289 行代码

---

## 5. 三种数据库分工

| 数据库 | 用途 | 原因 |
|--------|------|------|
| **openGauss** | 业务数据库 | 被管理的目标数据库 |
| **GaussDB Vector** | 向量检索 | 存储知识库向量，支持相似度检索 |
| **SQLite** | 元数据存储 | 轻量级，存储会话历史、配置等 |

---

## 6. 技术栈

### 后端技术

| 组件 | 技术 | 说明 |
|------|------|------|
| **语言** | Python 3.x | 主要开发语言 |
| **框架** | FastAPI（封装层） | HTTP服务框架 |
| **数据库** | SQLite + 向量数据库 | 元数据+知识库 |
| **加密** | AES256-CBC | 密码加密存储 |
| **日志** | logging (多进程安全) | 日志管理 |

### AI/ML技术

| 组件 | 技术 | 说明 |
|------|------|------|
| **大模型** | Pangu/ChatGLM/Baichuan/Llama | LLM推理 |
| **Embedding** | BGE | 文本向量化 |
| **Reranker** | BGE-Reranker | 结果重排序 |

### 安全技术

| 组件 | 技术 | 说明 |
|------|------|------|
| **传输加密** | SSL/TLS | HTTPS传输 |
| **密码加密** | AES256-CBC | 静态数据加密 |
| **敏感词检测** | DFA算法 | 内容安全 |

---

## 7. 核心设计模式

| 模式 | 应用场景 | 代码位置 |
|------|---------|---------|
| **装饰器模式** | `@base_tools` 注册工具 | `dbmind_interface.py` |
| **注册表模式** | `tools_registry = {}` 管理所有工具 | `global_vars.py` |
| **工厂模式** | `instantiate_llm()` 根据名称创建LLM | `llms/` |
| **模板方法** | `BaseAgent.interaction()` 抽象方法 | `base_agent.py` |
| **观察者/生成器** | `async for ... yield` 流式输出 | `dba.py` |

---

## 8. 关键特性总结

### 核心优势

1. **AI原生态**: 深度整合大语言模型，实现自然语言运维
2. **工具自动化**: 智能工具匹配与调用，减少人工操作
3. **知识驱动**: 基于知识库的精准问答，覆盖运维全场景
4. **多模型支持**: 兼容主流大模型，灵活切换
5. **安全可靠**: 端到端加密，敏感词检测，密码强度校验
6. **分布式架构**: 支持多集群管理，可扩展部署

### 设计原则

- **模块化**: 各组件解耦，便于维护和扩展
- **可配置**: 多层次配置项，满足不同部署需求
- **安全性**: 密码加密传输，敏感词过滤
- **容错性**: 完善的异常处理与日志记录
- **可观测性**: 详细的日志输出，便于问题排查

---

## 9. 未来演进方向

1. **更多LLM支持**: 接入更多国产大模型
2. **智能诊断增强**: 引入更多诊断模型
3. **自动化修复**: 从诊断建议到自动执行
4. **多数据库支持**: 从openGauss扩展到其他数据库
5. **可视化增强**: 更丰富的Web UI
6. **运维生态**: 与更多运维工具集成

---

## 10. 参考资源

- **openGauss官网**: https://www.opengauss.org/
- **DBMind项目**: https://gitcode.com/opengauss/openGauss-DBMind
- **Mulan PSL v2许可证**: http://license.coscl.org.cn/MulanPSL2/

---

*本文档基于 openGauss-GaussMaster v1.0.0*
